name = "postai-api"
main = "src/index.ts"
compatibility_date = "2024-04-19"
compatibility_flags = ["nodejs_compat"]

[vars]
# Variáveis de ambiente públicas (ou placeholders)
ENVIRONMENT = "development"

# Bindings de infraestrutura conforme arquitetura.md
# [hyperdrive]
# binding = "DATABASE"
# id = "PLACEHOLDER_ID"

# [r2_buckets]
# binding = "R2"
# bucket_name = "postai-media"

# [queues]
# binding = "QUEUE"
# queue_name = "postai-ops-queue"
